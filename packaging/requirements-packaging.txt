# Build-time dependencies
# Prefer current PyInstaller (numpy.libs / delvewheel DLL paths).
# Windows CI may AV while analyzing pywinauto — jarvis.spec excludes it from
# Analysis and copies the package tree as data instead.
pyinstaller>=6.14.0,<7
